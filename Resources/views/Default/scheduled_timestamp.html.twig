{% extends 'BCCResqueBundle::layout.html.twig' %}
{% block body %}
{% include "BCCResqueBundle:Default:_menu.html.twig" with {activeNav: 'none'} %}
<div class="container">
    <div class="row span12">
        <h2>Jobs Scheduled for {{ timestamp|date('Y-m-d H:i:s') }}</h2>
        <p><a href="{{ path('BCCResqueBundle_scheduled_list') }}">Back to all Scheduled Jobs</a></p>
        <table class="table table-striped table-bordered">
            <thead>
            <tr>
                <th>Job</th>
                <th>Details</th>
            </tr>
            </thead>
            <tbody>
            {% for job in jobs %}
                <tr>
                    <th class="span1">{{ loop.index }}</th>
                    <td class="span11">
                        <pre>{{ job }}</pre>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
